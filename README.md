# AI USE DISCLAIMER
### The following contents/code are AI assisted/generated
- Regex patterns for matching
- README.md

# Live Markdown Converter & Editor

A lightweight, browser-based tool that instantly translates raw Markdown into HTML code and renders a live visual preview. Built with vanilla JavaScript, this editor features a flexible, dynamic UI designed to adapt to your workflow.

## Core Functionality: Markdown Conversion

At the heart of the application is a custom regex-based parsing engine that translates Markdown syntax into valid HTML tags in real-time as you type. 

**Supported Markdown Syntax:**
*   **Headings:** H1 to H3 (`#`, `##`, `###`)
*   **Text Formatting:** Bold (`**` or `__`) and Italic (`*` or `_`)
*   **Blockquotes:** (`> quote`)
*   **Links:** Standard inline links (`[text](url)`)
*   **Images:** Standard image embeds (`![alt](url)`)

The conversion logic strictly respects the order of execution (e.g., parsing `###` before `#`, and images before links via negative lookbehinds) to prevent HTML tag collisions, ensuring clean and accurate outputs.

## Dynamic UI: Expandable Panels & Tabs

To accommodate different coding and writing styles, the interface is divided into three distinct panels:
1.  **Markdown Input:** The text area where you write your raw Markdown.
2.  **HTML Result:** The parsed, raw HTML code generated by the engine.
3.  **Preview Output:** The visually rendered final webpage.

**Workspace Control Features:**
*   **Maximize and Minimize Content:** You can expand individual content containers (Input, HTML, or Preview) to take over the screen for a distraction-free workflow. When maximized, the UI leverages CSS Flexbox to fill the viewport.
*   **Responsive Layout:** The grid automatically manages space allocation. Empty or minimized panels collapse gracefully, while textareas intelligently expand to accommodate their text without breaking the layout boundaries.
